name: kt_stdlib
description: Brings Kotlins standard library to Dart, including colletions (List, Set, Map). Unlike Darts these collections are immutable by default but offer special `Mutable` types.
version: 0.4.0-alpha1
homepage: https://github.com/passsy/dart_kollection
author: Pascal Welsch <pascal.welsch@gmail.com>

environment:
  sdk: '>=2.0.0 <3.0.0'

dev_dependencies:
  test: ^1.0.0
